<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page to Tyler's Tech Blog</title>
    <link rel="stylesheet" href="/public/style.css"> <!-- Update the CSS file path -->
</head>
<body>
    <header class="header">
        <h1><a href="/">My Blog</a></h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                {{#if user}}
                    <li><a href="/dashboard">Dashboard</a></li>
                    <li><a href="/logout">Logout</a></li>
                {{else}}
                    <li><a href="/login">Login/Sign Up</a></li>
                {{/if}}
            </ul>
        </nav>
    </header>

    <main class="container">
        {{#if posts}}
            {{#each posts}}
                <article class="post">
                    <h2>{{title}}</h2>
                    <p>{{content}}</p>
                    <p>Created by {{author}} on {{date}}</p>
                    <!-- Include comments and related content here -->
                </article>
            {{/each}}
        {{else}}
            <p>No blog posts yet.</p>
        {{/if}}
    </main>

    <footer class="footer">
        <p>&copy; 2023 Tyler's Tech Blog</p>
    </footer>
</body>
</html>
