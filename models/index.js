const User = require('./User');

module.exports = { User, login: async (username, password) => {

},
logout: (user) => {

},
register: async (username, password) => {

},
checkCredentials: (user) => {
    
} };

// TODO: Implement methods to handle  user authentication, such as login and logout